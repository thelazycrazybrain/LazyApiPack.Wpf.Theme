<Window x:Class="LazyApiPack.XamlPreview.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LazyApiPack.XamlPreview"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Button Content="Next" DockPanel.Dock="Right" Command="{Binding NextCommand}"/>
        <TextBlock Text="{Binding CurrentName}" DockPanel.Dock="Bottom"/>
        <Viewbox>
            <ContentControl Content="{Binding CurrentResource}" />
        </Viewbox>

    </DockPanel>
</Window>
